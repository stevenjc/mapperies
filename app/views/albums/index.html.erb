<%= link_to 'Create New Album', action: "create", remote:true%>
<br>

<p id="a"> Some sample text </p>

<section id="portfolio" class="portfolio">
        <div class="container">
            <div class="row">
                <div class="col-lg-10 col-lg-offset-1 text-center">
                    <h2>My Albums</h2>
                    <hr class="small">
                    <div class="row">

                      <table>
                          <tbody>
                            <%= render "index" %>
                          </tbody>
                        </table>
                    <!-- /.row (nested) -->
                    <a href="#" class="btn btn-dark">View More Items</a>
                </div>
                <!-- /.col-lg-10 -->
            </div>
            <!-- /.row -->
        </div>
        <!-- /.container -->
    </section>
    <div id="product-modal" class="modal fade"></div>
