<h1> Upload Photo</h1>
<%= form_for @photo, html: {multipart: true} do |f| %>
  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <%= f.submit 'Upload' %>
<% end %>
