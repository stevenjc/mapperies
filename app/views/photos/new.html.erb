<h1> Upload Photo to current album...</h1>

<%= image_tag @photo.image.url(:med) %>
<%= @photo.image.url(:med) %>

<%= form_for @photo, html: {multipart: true} do |f| %>
<!--
  <div class="form-group">
    <%= f.label :album_id %>
    <%= f.text_field :album_id %>
  </div>

  <div class="form-group">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div> -->

  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <%= f.submit 'Upload' %>
<% end %>
